all:

install:
	$(INSTALL_DIR) $(DESTDIR)$(BINDIR)
	$(INSTALL_PROGRAM) agoipx800 $(DESTDIR)$(BINDIR)
	$(INSTALL_PROGRAM) pyipx800v3.py $(DESTDIR)$(BINDIR)
	$(INSTALL_DIR) $(DESTDIR)$(CONFDIR)

clean:
